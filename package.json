{
  "name": "grav-well",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Gravity Well — a faithful falling-block stacker for Paul's Arcade.",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "test": "node tools/verify-artifact.mjs && node --test 'tests/*.test.js'",
    "stage": "node tools/stage.mjs dist"
  }
}
